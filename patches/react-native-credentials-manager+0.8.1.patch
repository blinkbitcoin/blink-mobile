diff --git a/node_modules/react-native-credentials-manager/android/src/newarch/java/com/credentialsmanager/CredentialsManagerModule.kt b/node_modules/react-native-credentials-manager/android/src/newarch/java/com/credentialsmanager/CredentialsManagerModule.kt
index 664814d..b5139d2 100644
--- a/node_modules/react-native-credentials-manager/android/src/newarch/java/com/credentialsmanager/CredentialsManagerModule.kt
+++ b/node_modules/react-native-credentials-manager/android/src/newarch/java/com/credentialsmanager/CredentialsManagerModule.kt
@@ -6,6 +6,7 @@ import androidx.credentials.exceptions.GetCredentialException
 import androidx.credentials.exceptions.NoCredentialException
 import com.credentialsmanager.handlers.CredentialHandler
 import com.credentialsmanager.handlers.ErrorHandler
+import com.facebook.react.bridge.Arguments
 import com.facebook.react.bridge.Promise
 import com.facebook.react.bridge.ReactApplicationContext
 import com.facebook.react.bridge.ReadableArray
@@ -67,12 +68,21 @@ class CredentialsManagerModule(
       try {
         credentialHandler.createPassword(username, password)
 
-        // Create success response
-        val result = mapOf(
-          "type" to "password",
-          "username" to username,
-          "success" to true
-        )
+        /**
+         * Patched (blink-mobile): upstream resolved with `mapOf(...)`, which Kotlin
+         * builds as a `LinkedHashMap`. `Arguments.fromJavaArgs` does not bridge raw
+         * `LinkedHashMap` to JS and throws `RuntimeException: Cannot convert
+         * argument of type class java.util.LinkedHashMap`, crashing the app right
+         * after the Android password-manager Save dialog. Rebuilding the same
+         * response shape via `Arguments.createMap()` satisfies the bridge contract.
+         * Upstream: https://github.com/benjamineruvieru/react-native-credentials-manager/issues/23
+         * (remove this patch once a release includes the fix).
+         */
+        val result = Arguments.createMap().apply {
+          putString("type", "password")
+          putString("username", username)
+          putBoolean("success", true)
+        }
         promise.resolve(result)
       } catch (e: CreateCredentialException) {
         ErrorHandler.handleCredentialError(e)
diff --git a/node_modules/react-native-credentials-manager/android/src/oldarch/java/com/credentialsmanager/CredentialsManagerModule.kt b/node_modules/react-native-credentials-manager/android/src/oldarch/java/com/credentialsmanager/CredentialsManagerModule.kt
index e35cdb3..301a7c8 100644
--- a/node_modules/react-native-credentials-manager/android/src/oldarch/java/com/credentialsmanager/CredentialsManagerModule.kt
+++ b/node_modules/react-native-credentials-manager/android/src/oldarch/java/com/credentialsmanager/CredentialsManagerModule.kt
@@ -6,6 +6,7 @@ import androidx.credentials.exceptions.GetCredentialException
 import androidx.credentials.exceptions.NoCredentialException
 import com.credentialsmanager.handlers.CredentialHandler
 import com.credentialsmanager.handlers.ErrorHandler
+import com.facebook.react.bridge.Arguments
 import com.facebook.react.bridge.Promise
 import com.facebook.react.bridge.ReactApplicationContext
 import com.facebook.react.bridge.ReactContextBaseJavaModule
@@ -73,12 +74,21 @@ class CredentialsManagerModule(
       try {
         credentialHandler.createPassword(username, password)
 
-        // Create success response
-        val result = mapOf(
-          "type" to "password",
-          "username" to username,
-          "success" to true
-        )
+        /**
+         * Patched (blink-mobile): upstream resolved with `mapOf(...)`, which Kotlin
+         * builds as a `LinkedHashMap`. `Arguments.fromJavaArgs` does not bridge raw
+         * `LinkedHashMap` to JS and throws `RuntimeException: Cannot convert
+         * argument of type class java.util.LinkedHashMap`, crashing the app right
+         * after the Android password-manager Save dialog. Rebuilding the same
+         * response shape via `Arguments.createMap()` satisfies the bridge contract.
+         * Upstream: https://github.com/benjamineruvieru/react-native-credentials-manager/issues/23
+         * (remove this patch once a release includes the fix).
+         */
+        val result = Arguments.createMap().apply {
+          putString("type", "password")
+          putString("username", username)
+          putBoolean("success", true)
+        }
         promise.resolve(result)
       } catch (e: CreateCredentialException) {
         ErrorHandler.handleCredentialError(e)
